/**
 * Controllers for handling characters HTTP request. Controller responsibility is to fetch client input, convert it to
 * data types used on business logic level, call business logic, convert result to data types used in communication with
 * client.
 */
package com.example.skins.user.controller;
